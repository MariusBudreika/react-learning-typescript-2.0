import React from "react";

// More coxplex types : arrays, objects (TYPESCRIPT)

const ArraysObjects = () => {
  let hobbies: string[];
  hobbies = ["Hiking", "Swimming"];

  let person: {
    name: string;
    age: number;
  };

  person = {
    name: "Marius",
    age: 28,
  };

  let people: {
    name: string;
    age: number;
  }[];

  return <div></div>;
};

export default ArraysObjects;
